flask==3.1.3
# NOTE: numpy 1.26.2 / scipy 1.13.1 ship wheels only up to CPython 3.12.
# On 3.13+ pip falls back to a source build that fails (meson). Use Python
# 3.12 exactly until these pins are bumped to versions with 3.13 wheels.
matplotlib==3.9.2
numpy==1.26.2
scipy==1.13.1
pandas==2.2.3
seaborn==0.13.2
yfinance==0.2.61
python-dotenv==1.1.1
# Optional: required only when AUTO_UPDATE_TICKERS is set (imported lazily by
# data_pipeline/scheduler.py). See docs/constraints.md §6.
APScheduler==3.10.4
# Production WSGI server. Required by Render's start command
# (`gunicorn --bind 0.0.0.0:$PORT app:app`). Also installed in deploy/Dockerfile.
gunicorn==26.2.0

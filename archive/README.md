# archive/ — retired code, read-only

Historical modules kept for reference only. Nothing here is imported by the
runtime (`app.py → routes/ → services/ → core/ → data_pipeline/ → utils/`),
covered by tests, or scanned by `scripts/doc_guard.py` / `arch_metrics.py`.

Do not revive files by editing them in place — copy what you need into the
proper layer and adapt it. Candidates for deletion whenever the repo needs
slimming; deletion requires no migration since nothing depends on them.
